# Optional: A GitHub Personal Access Token (classic or fine-grained, no scopes needed
# for public data) raises the GitHub API rate limit from 60 requests/hour to 5000/hour.
# Create one at https://github.com/settings/tokens
# Leave blank to use the app without a token (fine for personal/local use).
GITHUB_TOKEN=
